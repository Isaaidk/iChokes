# iChokes

Este repositorio contiene el proyecto `roboflow-object-detection-app`.

Para desplegar en Railway, Railway debe usar el `Dockerfile` en la raíz del repositorio.

Asegúrate de configurar las variables de entorno en Railway:
- ROBOFLOW_API_KEY
- MODEL_ID
- API_URL=https://serverless.roboflow.com
